name: a-core-build

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1

    - name: |
        cd ./a-core/hello-demo
        docker build --tag staranto/junk --file Dockerfile.hello .

